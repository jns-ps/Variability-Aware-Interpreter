begin
// #if FOO
class A {
  var a;
  
  // #if BAR
  def add(x) {
    res = 10 + x;
  }
  // #endif
}
// #endif

// #if OOF
class A {
  var a;
  
  // #if BAR
  def add(x) {
    this.a = this.a + x;
  }
  // #endif
}
// #endif

o = new A(2);
// #ifdef ERR
o = 700;
// #endif

o.add(10);
y = o.a;

end